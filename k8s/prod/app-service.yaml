apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: prod
spec:
  type: NodePort
  selector:
    app: devops-demo
    env: prod
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30082
apiVersion: v1
kind: Service
metadata:
  name: person-service
  namespace: prod
spec:
  type: ClusterIP
  selector:
    app: person-service
    env: prod
  ports:
    - port: 8082
      targetPort: 8082

apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: uat
spec:
  type: NodePort
  selector:
    app: devops-demo
    env: uat
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081